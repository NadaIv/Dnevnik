@model IEnumerable<Dnevnik.Models.Odeljenja>

@{
    ViewBag.Title = "Odeljenjas";
}

<h2>Odeljenja</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Ime)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Budzet)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.StartDate)
        </th>
        <th>
            Administrator
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Ime)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Budzet)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.StartDate)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Administrator.ImeIPrezime)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.OdeljenjeID }) |
            @Html.ActionLink("Details", "Details", new { id=item.OdeljenjeID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.OdeljenjeID })
        </td>
    </tr>
}

</table>
